# Keras backend to use (required)
KERAS_BACKEND=jax

# MLflow tracking server URI (required)
# Local example: http://127.0.0.1:5000
# Remote example: http://ec2-public-ip:5000
MLFLOW_TRACKING_URI=http://127.0.0.1:5000

# AWS Configuration (required for AWS deployments)
AWS_USERNAME=your-aws-username
AWS_ROLE=arn:aws:iam::123456789012:role/mlschool
AWS_REGION=us-east-1
BUCKET=mlschool-bucket-name

# Endpoint Configuration (required for deployments)
ENDPOINT_NAME=penguins

# Azure Configuration (required for Azure deployments)
AZURE_SUBSCRIPTION_ID=your-subscription-id
AZURE_RESOURCE_GROUP=your-resource-group
AZURE_WORKSPACE=your-workspace-name

# Optional: Data Collection URI for local model deployments
# DATA_COLLECTION_URI=sqlite:///path/to/your/database.db
